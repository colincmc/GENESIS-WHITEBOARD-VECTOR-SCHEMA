{
  "name": "genesis-whiteboard-vector-schema",
  "version": "1.0.0",
  "description": "Vector embedding schema for Whiteboard entries. Dimension, distance metric, index config. When NeMo Retriever arrives, Whiteboard becomes RAG-ready. Port 8789.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.11.5",
    "@types/express": "^4.17.21"
  }
}
